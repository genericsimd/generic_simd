1. To build three versions

make  /* build scalar version */
make SSE=1 /*build Intel SSE based SIMD version*/
make SIMD=1 /* build Generic SIMD version for power and intel sse */

2. To run scalar and SIMD versions

./cva_kernel
./cvs_kernel_sse
./cva_kernel_simd
